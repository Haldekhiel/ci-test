name: Readme Checker

on:
  pull_request:
     branches:
        - main

jobs:
  readmechecker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
          if test -f README.md
          then
            echo "readme exists."
          else
            exit 1
          fi
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: super-linter/super-linter-action@v4
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}           
          VALIDATE_PYTHON: true

